# EDP Node backend (run: npm run backend from repo root, or cd backend && npm start)
REACT_APP_EDP_API_URL=http://localhost:5000

# Local Grafana (started via daire-phase-1/start.sh)
REACT_APP_GRAFANA_URL=http://localhost:3001
